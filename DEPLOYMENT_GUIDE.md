# 🚀 Romax Call - Deployment Guide

## Backend на Render.com

### Что готово:

✅ **server.ts** - обновлен для production:
  - Слушает на `0.0.0.0:PORT` (совместимо с облаком)
  - Улучшенный CORS (поддерживает несколько доменов)
  - Graceful shutdown обработка
  - Логирование для мониторинга

✅ **render.yaml** - конфигурация для автоматического развёртывания

✅ **.env.example** - пример переменных окружения

### Шаги развёртывания:

#### 1. Подготовка Render
1. Зайди на https://render.com
2. Зарегистрируйся через GitHub
3. Подключи свой GitHub репозиторий

#### 2. Создание web сервиса
1. Нажми "New +" → "Web Service"
2. Выбери свой репозиторий
3. Заполни:
   - **Name:** romax-call-api
   - **Environment:** Node
   - **Build Command:** `npm install && npx tsc --project tsconfig.server.json`
   - **Start Command:** `node dist_backend/server.js`
   - **Plan:** Free

#### 3. Добавление переменных окружения
1. В разделе "Environment" добавь:
   - `NODE_ENV` = `production`
   - `DATABASE_URL` = `postgresql://...` (из Neon)
   - `SESSION_SECRET` = (длинная случайная строка)
   - `CORS_ORIGIN` = `https://romax-call.vercel.app`
   - `FRONTEND_URL` = `https://romax-call.vercel.app`

#### 4. Deploy
1. Нажми "Create Web Service"
2. Дождись успешного развёртывания (5-10 минут)
3. Скопируй URL вида `https://romax-call-api.onrender.com`

### Проверка работы

После развёртывания проверь:

```bash
# Health check
curl https://romax-call-api.onrender.com/health

# Должен вернуть:
# {"status":"ok"}
```

### Переменные окружения для фронтенда

Обнови в Vercel:
```
REACT_APP_API_URL=https://romax-call-api.onrender.com
REACT_APP_SOCKET_URL=https://romax-call-api.onrender.com
```

## Важно знать

⚠️ **Бесплатный план Render:**
- Сервис засыпает через 15 минут неактивности
- Просыпается за 30 секунд при первом запросе
- Этого достаточно для тестирования с друзьями

💡 **Если нужна постоянная работа:**
- Обнови на платный план ($7/месяц)
- Или используй Render Cron Jobs для пинга сервиса каждые 14 минут

## Готово! 🎉

Когда backend развернулся, переходим к **Промту 7: Подключение приложения к облачному API**
